# WHOAREYOU 2.0 런타임 의존성 (원클릭 인스톨러가 uv로 설치)
# torch는 Windows PyPI 휠이 CPU 빌드 → 별도 인덱스 불필요. 모델(bge-reranker·easyocr)은 부트스트랩이 워밍 다운.

# ── 데스크톱/트레이/알림 ──
pywebview==6.2.1
pystray==0.19.5
pillow==10.4.0
winotify==1.1.0

# ── 크롤/파싱 ──
httpx==0.27.2
curl_cffi==0.15.0
lxml==6.1.1

# ── 이력서·어휘 매칭 ──
pdfplumber==0.11.9
kiwipiepy==0.23.1
kiwipiepy_model==0.23.0
rapidfuzz==3.14.5

# ── 스케줄러 ──
APScheduler==3.11.3

# ── 의미 매칭(리랭커) — heavy ──
numpy==2.4.6
torch==2.12.1
torchvision==0.27.1
transformers==4.57.6
tokenizers==0.22.2
safetensors==0.8.0
huggingface_hub==0.36.2
sentence-transformers==5.6.0

# ── 이미지 공고 OCR — heavy ──
easyocr==1.7.2
